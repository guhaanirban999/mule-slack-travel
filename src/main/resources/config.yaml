http:
  # Port the HTTP listener binds to (CloudHub 2.0 maps inbound 443 -> 8081)
  port: "8081"
broker:
  # Host of the deployed Travel Agent Broker (A2A JSON-RPC 2.0 endpoint)
  host: "travel-agent-broker-network-8u1hpn.5sc6y6-2.usa-e2.cloudhub.io"
  port: "443"
